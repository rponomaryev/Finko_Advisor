import { buildDynamicInterviewTemplate } from "../../interview/dynamicInterviewEngine.ts";

export const template = buildDynamicInterviewTemplate({ businessType: "Магазин косметики", businessIdea: "Adaptive sector template fixture" });
export default template;
