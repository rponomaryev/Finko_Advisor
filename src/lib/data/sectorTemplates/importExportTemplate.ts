import { buildDynamicInterviewTemplate } from "../../interview/dynamicInterviewEngine.ts";

export const template = buildDynamicInterviewTemplate({ businessType: "Импорт оборудования", businessIdea: "Adaptive sector template fixture" });
export default template;
