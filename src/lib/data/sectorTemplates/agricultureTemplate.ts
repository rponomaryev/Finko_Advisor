import { buildDynamicInterviewTemplate } from "../../interview/dynamicInterviewEngine.ts";

export const template = buildDynamicInterviewTemplate({ businessType: "Теплица", businessIdea: "Adaptive sector template fixture" });
export default template;
