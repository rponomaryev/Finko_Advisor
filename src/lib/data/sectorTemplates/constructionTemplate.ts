import { buildDynamicInterviewTemplate } from "../../interview/dynamicInterviewEngine.ts";

export const template = buildDynamicInterviewTemplate({ businessType: "Строительная компания", businessIdea: "Adaptive sector template fixture" });
export default template;
